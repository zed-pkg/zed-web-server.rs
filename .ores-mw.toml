schema_version = 1
repository_mode = "server-only"
default_target = "web"

[[targets]]
name = "web"
role = "server"
roots = ["src"]
middleware = "stack"
stack_config = "config/ores-middleware-stack.json"
