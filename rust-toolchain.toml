# Pin the toolchain so CI cache keys stay stable and every machine builds the
# same way (zed-docs issue #8). Keep this synchronized with the Docker build
# stage's RUSTUP_TOOLCHAIN so hosted and container builds compile the same source
# with the same Rust release.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
