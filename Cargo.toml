[package]
name = "zed-web-server"
version = "0.1.0"
edition = "2024"
description = "zed-pkg registry web UI: MASH stack (Maud, Axum, SeaORM, HTMX)"
license = "MIT"
repository = "https://github.com/zed-pkg/zed-web-server.rs"

[dependencies]
k8s-web-api-data-plane = { git = "https://github.com/ORESoftware/k8s-libs-and-shared-defs.git", rev = "55adadc9f6d223d40b28ae66097dfe16930951b1", package = "k8s-web-api-data-plane" }
# Keep the shared wire contract immutable. The repository root is a virtual
# polyglot workspace, and Cargo resolves its Rust package member directly.
zed-interfaces = { git = "https://github.com/zed-pkg/zed-interfaces.git", rev = "7d31f80dd8a310f218931165a3ad636a2f32b932" }
# Default features only: the read-only surface. Enabling `read-write` here
# would hand this process write symbols its database identity cannot use.
# Pin the certified canonical data-plane revision; never follow a moving branch
# in a production server lock graph.
zed-orm-core = { git = "https://github.com/zed-pkg/zed-lib-core.git", rev = "9ca75929b0be79aaa0b8af60c2810a8ac0c34198" }
chrono = { version = "0.4", default-features = false, features = ["clock"] }
uuid = { version = "1", features = ["v4"] }
anyhow = "1"
axum = "0.8"
dotenvy = "0.15"
http-body = "1"
maud = "0.27"
# No default features: no auto-redirects wanted (policy is set to none at
# construction anyway) and auth pages must pass through byte-for-byte, so no
# decompression either. `stream` is for piping upstream bodies back verbatim.
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream", "json"] }
semver = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["trace", "fs", "set-header", "timeout", "catch-panic"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
